const links=[...document.querySelectorAll('nav a')];const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>a.boundingClientRect.top-b.boundingClientRect.top);if(visible.length)links.forEach(link=>{const active=link.hash==='#'+visible[0].target.id;link.classList.toggle('active',active);if(active)link.setAttribute('aria-current','location');else link.removeAttribute('aria-current')})},{rootMargin:'-80px 0px -55% 0px',threshold:0});document.querySelectorAll('.menu-category').forEach(s=>observer.observe(s));